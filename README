1. Поправьте в munin-export.conf пути и поместите его в /etc/apache2/conf.d:
> ln -s munin-export.conf /etc/apache2/conf.d/munin-export.conf
2. Поправьте в export.py пути к БД Munin'a.
3. Перезапустите Apache.
> service apache2 reload
4. Получайте XML-выдачу по адресу http://localhost/munin-export/export.py?type=users&period=hour.
